
import session from '../assets/js/session';

export default ({app, redirect, store }) => {
  // if (store.state.global.token.value === '') {
  //   redirect('/login');
  // }
  // if (process.client) {
  //   if (!!!session.getString('token')) {
  //     redirect('/login');
  //   }
  // }
  // app.mounted(() => {
  //   console.log(123);
  // })
};
